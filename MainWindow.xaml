<Window x:Class="LSL_Kinect.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LSL_Kinect"
        mc:Ignorable="d"
        Title="Kinect Capture"
        Loaded="MainWindow_Loaded"
        Closing="Window_Closing"
        Height="750" Width="800"
        KeyUp="Window_KeyUp">
    <Window.Resources>
        <SolidColorBrush x:Key="MediumGreyBrush" Color="#ff6e6e6e" />
        <SolidColorBrush x:Key="KinectPurpleBrush" Color="#ff52318f" />
        <SolidColorBrush x:Key="KinectBlueBrush" Color="#ff00BCF2" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" MinHeight="500"/>
            <RowDefinition Height="*" />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>

        <!--Upper part-->
        <Image Grid.Row="1" Grid.Column="1" Name="camera"  MaxHeight="500" Stretch="Uniform"/>
        <Canvas Grid.Row="1" Grid.Column="1"  Name="canvas" MaxHeight="500" MaxWidth="500" />

        <!-- Inner bottom grid-->
        <Grid Grid.Row="2" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Row 0 -->
            <StackPanel Grid.Row="0" Grid.Column="0"  Grid.ColumnSpan="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="5">
                <Label Content="Skeletton ID : " Margin="10" />
                <TextBlock x:Name="trakingIdCourt" Text="-----" Margin="10" VerticalAlignment="Center" />
                <Label Content="Complete Skeletton ID : " Margin="10" />
                <TextBlock x:Name="trakingIdFull" Text="-----" Margin="10" VerticalAlignment="Center" />
            </StackPanel>

            <!-- Row 1 -->
            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="5">
                <Button x:Name="LSLMarkerSendButton" Content="Send LSL Marker" Width="Auto"  Click="OnSendLSLClicked"  Margin="5" />
                <Label Content="LSL Marker sent : " VerticalAlignment="Center" Margin="5" />
                <TextBlock x:Name="LslNumberSpaceBarPress" Text="-----" VerticalAlignment="Center" Margin="5" />
            </StackPanel>

            <!-- Row 2 -->
            <GroupBox  Grid.Row="2" Grid.Column="0" Header="Rendering Options" Margin="5">
                <StackPanel Grid.Column="0" Grid.Row="2"  Orientation="Horizontal">
                    <Button x:Name="color_btn" Grid.Column="0" Content="Couleur" Margin="5" Click="Button_Click_Color" />
                    <Button x:Name="depth_btn" Grid.Column="1" Content="Profondeur" Margin="5" Click="Button_Click_Depth" />
                    <Button x:Name="infraRed_btn" Grid.Column="2" Content="Infrarouge" Margin="5" Click="Button_Click_IR" />
                    <Button x:Name="VisuTracking_btn" Grid.Column="3" Content="Visu Tracking" Margin="5" Click="Button_Click_Body" />
                </StackPanel>
            </GroupBox>

            <GroupBox Grid.Row="2" Grid.Column="1" Header="CSV" Margin="5">
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="record_btn" Content="Start Record" Click="OnRecordButtonClicked" Margin="5" />
                    <Button x:Name="CSV_btn" Content="Export CSV" Click="CSV_btn_Click" IsEnabled="False" Margin="5" />
                </StackPanel>
            </GroupBox>
        </Grid>

        <!-- Not working
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Path=etat_Path, UpdateSourceTrigger=PropertyChanged}" />

        <StatusBar Grid.Row="4" HorizontalAlignment="Stretch" Name="statusBar" VerticalAlignment="Bottom" Background="White" Foreground="{StaticResource MediumGreyBrush}" Height="26">
            <StatusBarItem>
                <TextBlock Text="{Binding Path=etat_Kinect, UpdateSourceTrigger=PropertyChanged}" />
            </StatusBarItem>
            <StatusBarItem Height="26" />
        </StatusBar>
        -->
    </Grid>
</Window>